version: 2
updates:
  # .NET / NuGet packages (central package management: Directory.Packages.props).
  - package-ecosystem: "nuget"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "deps"
    labels:
      - "dependencies"
      - "nuget"
    groups:
      # One PR for non-breaking bumps, merged automatically once CI is green (see
      # workflows/dependabot-auto-merge.yml). Majors still get individual PRs, and are never
      # auto-merged.
      nuget-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # GitHub Actions used in the workflows.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
      - "github-actions"
    groups:
      # Same treatment as NuGet: grouped, auto-merged on green, majors reviewed by hand.
      github-actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
